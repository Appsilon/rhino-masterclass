describe('app', () => {
  beforeEach(() => {
    cy.visit('/');
  });

  it('displays a title', () => {
    // Step 2:
    // ?
  });

  it('greets user with their name', () => {
    // Step 3:
    // ?

    // cy.get('#app-greeting')
    //   .contains('Hello Rhino!');
  });

  it('congratulates for checking all', () => {
    // Step 4:
    // ?

    // cy.get('#app-done')
    //   .contains('Well done!');
  });
});
